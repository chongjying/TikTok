// Example of LiveRewardsPage definition
import 'package:flutter/material.dart';

class LiveRewardsPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Live Rewards Page'),
      ),
      body: Center(
        child: Text('This is the Live Rewards Page'),
      ),
    );
  }
}
